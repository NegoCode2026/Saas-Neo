// Módulo recordatorios: no valida body/query con zod (solo GET / con ?dias numérico).
// Se mantiene este archivo para conservar la estructura uniforme <módulo>.routes + <módulo>.schemas.
export {};
