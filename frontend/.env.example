# PÚBLICO por diseño: todo NEXT_PUBLIC_* queda expuesto en el bundle del navegador.
# Aquí SOLO va la URL del API. NUNCA pongas JWT_SECRET, RESEND_API_KEY ni claves aquí.
NEXT_PUBLIC_API_URL="http://localhost:4000"
