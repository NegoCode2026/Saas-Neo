# Copiá a .env y generá un secreto real con:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# En producción (Render/Vercel) se configura como variable de entorno, no en archivos.
JWT_SECRET=""
